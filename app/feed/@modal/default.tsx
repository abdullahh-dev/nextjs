// This is a required file for parallel routes using @modal
// It renders nothing by default when no modal is active
const ModalDefault = () => {
  return null;  // or return <></> - both work the same
};

export default ModalDefault;
